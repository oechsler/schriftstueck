\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{style/layout}[Basic geometry layout for Schrifstueck]

% Geometry configuration
%--------------------------------------
\usepackage[
    top=3.8cm,
    right=2.5cm,
    left=2.5cm,
    headsep=0.8cm,
    headheight=16pt,
]{geometry}
%--------------------------------------

% Paragraph configuration
%--------------------------------------
\setlength{\parindent}{0em}
\setlength{\parskip}{1em}
%--------------------------------------
